// Email service stub - dependencies removed
// @react-email/components and resend have been removed from this project
// This file exists to prevent build errors from stale caches

export async function sendInquiryConfirmation(..._args: unknown[]) {
  console.log('Email service not configured - @react-email/components removed')
  return { success: false, error: 'Email service not configured' }
}

export async function sendNewPropertyAlert(..._args: unknown[]) {
  console.log('Email service not configured - @react-email/components removed')
  return { success: false, error: 'Email service not configured' }
}

export async function sendWeeklyNewsletter(..._args: unknown[]) {
  console.log('Email service not configured - @react-email/components removed')
  return { success: false, error: 'Email service not configured' }
}
